create or replace trigger t1
before update 
on e
for each row
begin
insert into trigger_table
values(:old.eno,:old.ename,:old.sal,user1,sysdate)
end;
/
     
create table trigger_table
   (
    no number(6),
   name varchar2(8),
    salary number(6),
    user1 varchar2(6),
    dtime timestamp
    );
 
create or replace trigger t1
 before update
 on e
 for each row
 begin
 insert into trigger_table
 values(:old.eno,:old.ename,:old.sal,user,systimestamp);
 end;
 /


create or replace trigger t1
 after update
 on e
 for each row
 begin
 insert into trigger_table
 values(:old.eno,:old.ename,:old.sal,user,systimestamp);
 end;
 /
